<nav class="navbar navbar-expand-lg navbar-dark">
	<div class="container-fluid-md container-lg">
		<a class="navbar-brand" href="{{ path('index') }}">Biograf</a>
		<button class="navbar-toggler border-0" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav">
				<li class="nav-item active">
					<a class="nav-link" href="#">Home
						<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Filmer</a>
				</li>
			</ul>

			{% if is_granted('IS_AUTHENTICATED_FULLY') %}
				<ul class="navbar-nav ml-auto">
					<li class="nav-item dropdown bg-light rounded-pill shadow-sm">
						<a class="nav-link btn-sm dropdown-toggle text-dark px-3" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<i class="fas fa-user fa-sm"></i>
							<span class="font-weight-bold ml-1 mr-2">{{ app.user.firstname }}</span>
							<i class="fas fa-chevron-down fa-sm text-muted"></i>
						</a>
						<div class="dropdown-menu dropdown-menu-right border-0 shadow-sm" aria-labelledby="navbarDropdown">
							<a class="dropdown-item" href="#">Mina sidor</a>
							<div class="dropdown-divider"></div>
							<a class="dropdown-item" href="{{ path('security_logout') }}">
								<i class="fas fa-sign-out-alt"></i>
								Logga ut
							</a>
						</div>
					</li>
				</ul>
			{% else %}
				<ul class="navbar-nav ml-auto">
					<li class="nav-item bg-light rounded-pill shadow-sm">
						<a class="nav-link text-dark px-3" href="{{ path('security_login') }}">
							<i class="fas fa-user fa-sm"></i>
							<span class="font-weight-bold ml-1 mr-2">Mina sidor</span>
						</a>
					</li>
				</ul>
			{% endif %}
		</div>
	</div>
</nav>
